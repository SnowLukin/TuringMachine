excluded: # paths to ignore during linting. Takes precedence over `included`.
  - Pods
  - Templates
identifier_name:
  excluded:
    - id
    - vm
force_unwrapping:
  severity: warning
force_cast:
  severity: warning
opt_in_rules:
  - force_unwrapping
